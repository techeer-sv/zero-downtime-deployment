http:
  routers:
    app-canary:
      rule: PathPrefix(`/`)
      entryPoints:
        - web
      service: app-canary

  services:
    app-canary:
      weighted:
        services:
          - name: app-b@docker
            weight: 95
          - name: app-a@docker
            weight: 5
        healthCheck: {}