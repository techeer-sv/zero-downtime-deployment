http:
  routers:
    app-canary:
      rule: PathPrefix(`/`)
      entryPoints:
        - web
      service: app-canary

  services:
    app-canary:
      weighted:
        services:
          - name: app-b@docker
            weight: 50
          - name: app-a@docker
            weight: 50
        healthCheck: {}